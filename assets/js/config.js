/* =====================================================================
   КОНФИГУРАЦИЯ САЙТА
   Всё, что вы захотите поменять «одной строкой», находится здесь.
   ===================================================================== */

window.APP_CONFIG = {

    /* ---------- ОБЩЕЕ ---------- */

    // Заголовок над названием теста на стартовом экране
    BRAND_TITLE: 'Итоговое тестирование',
    BRAND_SUBTITLE: 'Проверьте свои знания по теоретической части обучения',

    /* ---------- ПРОХОДНОЙ БАЛЛ ---------- */

    // Процент, начиная с которого тест считается пройденным.
    PASS_PERCENT: 80,

    /* ---------- ПЕРЕМЕШИВАНИЕ ---------- */

    // Перемешивать порядок вопросов
    SHUFFLE_QUESTIONS: false,
    // Перемешивать варианты ответов внутри вопроса
    // (типы matching и order защищены — их структура не ломается)
    SHUFFLE_OPTIONS: false,

    /* ---------- ПРАВИЛА ПОДСЧЁТА ---------- */

    // Частичный балл за сопоставление (matching): 0.5 за половину верных пар
    PARTIAL_MATCHING: true,
    // Частичный балл за последовательность (order)
    PARTIAL_ORDER: false,

    // Как считать вопросы, помеченные manualReview (открытые ответы без
    // автоматической проверки):
    //   'exclude'   — не входят в итоговый процент, выносятся отдельно (рекомендуется)
    //   'correct'   — засчитываются как верные
    //   'incorrect' — засчитываются как ошибка
    MANUAL_REVIEW_SCORING: 'exclude',

    /* ---------- ОЦЕНКА ВРЕМЕНИ ---------- */

    // Сколько минут закладывать на вопрос каждого типа (для стартового экрана)
    TIME_PER_TYPE: {
        single: 0.6,
        multiple: 0.8,
        matching: 1.2,
        order: 1.0,
        text: 2.0,
        situation: 2.0
    },

    /* ---------- TELEGRAM ---------- */

    // Адрес вашего serverless-эндпоинта (Cloudflare Worker и т. п.).
    // Токен бота здесь НЕ указывается — он живёт только внутри Worker'а.
    // Пока поле пустое, отправка просто не выполняется, тест работает как обычно.
    TELEGRAM_ENDPOINT: 'https://test-telegram-proxy.dev-dopamine05.workers.dev/',

    // Необязательный «ключ доступа» к эндпоинту. Отсекает случайный мусор.
    // Полноценной защитой не является (виден в исходном коде страницы).
    TELEGRAM_CLIENT_KEY: '',

    // Отправлять ли тексты открытых ответов в Telegram
    SEND_OPEN_ANSWERS_TO_TELEGRAM: true,

    // Таймаут отправки, мс
    TELEGRAM_TIMEOUT_MS: 12000,

    /* ---------- ПОВЕДЕНИЕ ---------- */

    // Предупреждать при попытке закрыть вкладку во время теста
    WARN_ON_LEAVE: true,

    // Максимальная длина имени
    NAME_MAX_LENGTH: 60,

    // Максимальная длина открытого ответа
    TEXT_MAX_LENGTH: 1200,

    // Показывать конфетти при успешном результате
    CONFETTI_ON_PASS: true
};
